name: Deploy Cosmos RBAC Removal Infrastructure

on:
  workflow_dispatch:
    inputs:
      location:
        description: 'Azure region'
        type: choice
        options:
          - eastus2
          - swedencentral
          - australiaeast
        default: eastus2
      workloadName:
        description: 'Workload name (used in resource naming)'
        type: string
        default: srelabcosmos

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      SCENARIO_ROOT: scenarios/cosmos-rbac-removal
      LOCATION: ${{ inputs.location || vars.AZURE_LOCATION || 'eastus2' }}
      WORKLOAD: ${{ inputs.workloadName || vars.WORKLOAD_NAME || 'srelabcosmos' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Create resource group
        run: |
          az group create \
            --name "rg-${{ env.WORKLOAD }}" \
            --location "${{ env.LOCATION }}" \
            --tags scenario=cosmos-rbac-removal environment=demo

      - name: Deploy Bicep template
        id: deploy
        run: |
          az deployment group create \
            --resource-group "rg-${{ env.WORKLOAD }}" \
            --template-file "${{ env.SCENARIO_ROOT }}/infra/bicep/main.bicep" \
            --parameters "${{ env.SCENARIO_ROOT }}/infra/bicep/main.bicepparam" \
              location="${{ env.LOCATION }}" \
              workloadName="${{ env.WORKLOAD }}" \
              tags='{"scenario":"cosmos-rbac-removal","environment":"demo"}' \
            --query 'properties.outputs' \
            -o json > deployment-outputs.json

          cat deployment-outputs.json

          echo "aks_name=$(jq -r '.aksClusterName.value' deployment-outputs.json)" >> "$GITHUB_OUTPUT"
          echo "cosmos_endpoint=$(jq -r '.cosmosDbEndpoint.value' deployment-outputs.json)" >> "$GITHUB_OUTPUT"
          echo "uami_client_id=$(jq -r '.uamiClientId.value' deployment-outputs.json)" >> "$GITHUB_OUTPUT"
          echo "law_id=$(jq -r '.logAnalyticsWorkspaceId.value' deployment-outputs.json)" >> "$GITHUB_OUTPUT"
          echo "appinsights_cs=$(jq -r '.appInsightsConnectionString.value' deployment-outputs.json)" >> "$GITHUB_OUTPUT"

      - name: Print deployment summary
        run: |
          echo "============================================"
          echo "  Cosmos RBAC Removal Infrastructure Complete"
          echo "============================================"
          echo "Resource Group:      rg-${{ env.WORKLOAD }}"
          echo "Location:            ${{ env.LOCATION }}"
          echo "AKS Cluster:         ${{ steps.deploy.outputs.aks_name }}"
          echo "CosmosDB Endpoint:   ${{ steps.deploy.outputs.cosmos_endpoint }}"
          echo "UAMI Client ID:      ${{ steps.deploy.outputs.uami_client_id }}"
          echo "Log Analytics ID:    ${{ steps.deploy.outputs.law_id }}"
          echo "App Insights CS:     ${{ steps.deploy.outputs.appinsights_cs }}"
          echo "Scenario guide:      ${{ github.server_url }}/${{ github.repository }}/blob/${{ github.sha }}/scenarios/cosmos-rbac-removal/README.md"
          echo "Next: Run 'Deploy Cosmos RBAC Removal Application' and follow scenarios/cosmos-rbac-removal/README.md."
          echo "============================================"
